// public/sw.js
// Minimal stub to silence browser 404 for service worker auto-discovery.
// Replace with a real Workbox/service worker if you need PWA support later.
self.addEventListener("install", () => self.skipWaiting());
self.addEventListener("activate", () => {});
